<div class="contaner">
    <% flash.each do |name,msg| %>
        <div class="alert alert-<%= name == "notice" ? "success" : "danger" %> alert-dismissible fade show" role="alert">            <%= msg %>
            <button type="button" class="close" data-dismissle="alert" arila-label="Close">
                <span area-hidden="true">&times;</span>
            </button>
        </div>
    <% end %>
</div>